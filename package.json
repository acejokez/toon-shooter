{
  "name": "toon-shooter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Toon Shooter — a post-apocalyptic 2D endless side-scrolling runner built on Three.js.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.160.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
